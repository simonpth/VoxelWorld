#ifndef CONSTANTS_H
#define CONSTANTS_H

namespace Constants
{
  const int TICK_RATE = 20;                      // ticks per second
  const int TICK_INTERVAL_MS = 1000 / TICK_RATE; // milliseconds per tick

  const int KEY_W = 87;
  const int KEY_S = 83;
  const int KEY_A = 65;
  const int KEY_D = 68;
  const int KEY_SPACE = 32;
  const int KEY_SHIFT = 16;

};

#endif // CONSTANTS_H